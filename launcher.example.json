{
  "_comment": "Example configuration - copy to launcher.json and adjust. Only app_name is required; everything else has a sensible default.",

  "app_name": "My App",
  "container_name": "my-app",
  "default_port": 8080,

  "health_check_path": "/api/health",
  "health_check_key": "status",
  "health_check_value": "ok",
  "health_check_timeout": 60,
  "browser_path": "/",
  "env_port_key": "APP_PORT",

  "compose_file": "docker-compose.prod.yml",
  "build_timeout": 600,
  "start_timeout": 120,
  "stop_timeout": 30,
  "estimated_build_steps": 0,

  "docker_desktop_path": "",
  "docker_install_url": "",

  "_comment_min_versions": "App-declared MINIMUM Docker environment (all optional, empty = not declared). The launcher's own intrinsic needs always apply (compose mode requires buildx >= 0.17 when compose >= 2.40.2); these can only RAISE the bar, never lower it. Prefer min_api_version over min_engine_version for engine features. Unparsable values are a hard error at startup.",
  "min_engine_version": "",
  "min_api_version": "",
  "min_compose_version": "",
  "min_buildx_version": "",

  "icon_path": "branding/app-icon.png",
  "tray_icon_path": "",
  "config_dir": "",
  "install_dir": "",

  "window_width": 620,
  "window_height": 470,
  "locale": "auto",
  "gui_backend": "tk",

  "repo_url": "https://github.com/owner/repo",
  "releases_url": "",
  "docs_url": "",

  "update_check_enabled": true,

  "single_instance": true,

  "tray_enabled": true,
  "tray_minimize_on_close": true,

  "show_advanced_ports": false,
  "internal_ports": {
    "backend": 8000,
    "nginx": 80
  },
  "env_internal_port_keys": {
    "backend": "APP_BACKEND_PORT",
    "nginx": "APP_NGINX_PORT"
  },

  "cleanup_on_start": true,
  "legacy_names": ["old-app-name"],
  "cleanup_configs": [
    "~/.old-app-name",
    "~/.config/old-app-name"
  ],
  "cleanup_search_paths": [
    "~/.config/",
    "~/.local/share/",
    "~/"
  ],

  "log_level": "INFO",
  "log_max_size": 5000000,
  "log_backup_count": 3,

  "custom_strings": {}
}
